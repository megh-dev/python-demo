pipeline {
    agent any
    stages {
        stage('Build image') {
            steps {
                echo 'Starting to build docker image'

                script {
                    sh "docker.build("my-image")
                   
                }
            }
        }
       stage('run') {
            steps {
                sh "docker run -d -p 5002:5000 --name meg my-image"
            }
        }    
    }
}

