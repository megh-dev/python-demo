pipeline {
    agent any
    stages {
        stage('Build image') {
            steps {
               sh "docker build -t my-image ."
            }
        }
       stage('run') {
            steps {
                sh "docker run -d -p 5003:5000 --name megs my-image"
            }
        }    
    }
}

